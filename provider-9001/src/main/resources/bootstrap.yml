server:
  port: 9001
  servlet:
    context-path: /provider
spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: NACOS-PROVIDER
  cloud:
    kubernetes:
      reload:
        enabled: true
        mode: event
      config:
        sources:
          - name: provider
            namespace: octopus
